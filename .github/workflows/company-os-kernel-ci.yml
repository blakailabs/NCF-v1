name: Company OS Kernel CI

on:
  push:
    branches:
      - feature/company-kernel-ha-persistence-v0.8
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: 08-COMPANY-OS/11-KERNEL-RUNTIME
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Validate HA Persistence Safety v0.8
        run: PYTHONPATH=. python scripts/validate_v08.py
